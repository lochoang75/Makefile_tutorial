bar = 
foo = $(bar)

all:
# Search for the "-i" flag. MAKEFLAGS is just list of single characters one per flag so "i" in this case .
ifneq (, $(findstring i, $(MAKEFLAGS)))
	echo "i was passed to MAKEFLAGS"
endif
